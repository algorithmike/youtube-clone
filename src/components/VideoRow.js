import React from 'react';
import '../styles/VideoRow.css';

const VideoRow = ({views, subs, timestamp, channel, description, title, image}) => {
    return (
        <div className="videoRow">
            
        </div>
    )
}

export default VideoRow;